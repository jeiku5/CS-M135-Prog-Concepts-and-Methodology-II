# Sort comparison

## Instructions

Fill in the sort methods.

You may switch from "Debug" builds to "Release" builds for faster execution.

"Debug" builds add some things that make the program easier to debug, but execution speed suffers.

You may add a `main()` function to run the sorting methods against your own data.
Running the project currently executes all tests. You may prefer to previous Google
Test assignments to see how to have one `main()` for running Google Tests and one
for executing the program with your own driver code.

